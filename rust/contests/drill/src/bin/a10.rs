use proconio::input;

fn main() {
    input! {
        s: String,
    }
    let upper = s.to_uppercase();
    println!("{}", upper);
}

わからん殺し。
// 普通にmdのヒント見て写経した。
// これ特定の文字だけとか、文の最初の一文字だけ大文字とか言われたら僕は死にそう。