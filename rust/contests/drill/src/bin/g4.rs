#![allow(unused)]
// 2Dグリッド克服ドリル G4: 上下左右4近傍の和（境界処理）
// 入力: H W / H行×W列の整数
// 出力: 各セルの上下左右(存在するものだけ)の和を、同じ形(H行×W列)で
// 機構: ① / 境界チェック（i>0, i+1<h, ...）
use proconio::input;

fn main() {
    // TODO: 自分で実装する
}
