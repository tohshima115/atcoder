#![allow(unused)]
// 2Dグリッド克服ドリル P2: 隣り合う '#' の組の数
// 入力: H W / H行の文字列（'#'/'.'、長さW）
// 出力: 横または縦に隣接する ('#','#') の組の数
// 機構: ① / 隣接アクセス（右と下だけ見れば重複しない）
use proconio::input;

fn main() {
    // TODO: 自分で実装する
}
