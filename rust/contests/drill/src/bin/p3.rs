#![allow(unused)]
// 2Dグリッド克服ドリル P3（発展・来週の道具）: 迷路の最短歩数（BFS）
// 入力: H W / H行の文字列（'.'通行可 '#'壁 'S'開始 'G'目標、長さW）
// 出力: S→G の最短歩数（上下左右1歩）、到達不能なら -1
// 機構: グリッド + dx/dy配列 + 2次元visited + VecDeque（BFS, 未習なら眺めるだけ）
use proconio::input;

fn main() {
    // TODO: 自分で実装する（BFS未習なら後回しでOK）
}
