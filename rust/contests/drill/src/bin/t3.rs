#![allow(unused)]
// 2Dグリッド克服ドリル T3: 各行で最初に '#' が出る列（1始まり、無ければ -1）
// 入力: H W / H行の文字列（'#'/'.'、長さW）
// 出力: 各行について最初の '#' の列番号(1始まり)、無ければ -1（H行）
// 機構: ③ / 行ごとに走査して見つけたら break
use proconio::input;

fn main() {
    // TODO: 自分で実装する
}
