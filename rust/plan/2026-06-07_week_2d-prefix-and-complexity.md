# 週間計画 2026-06-07（日）〜 06-13（土）【改訂：病欠対応 6/10〜】

## 状況
日(6/7)・月(6/8)・火(6/9)は病欠で消失（フル2日含む）。残りは **水・木(フル)・金・土** の4日。
→ **核だけに絞る。imos・C再実装・新道具箱（二分探索/DP）は来週送り。**

## 残す核（これだけは必達）
1. 2次元配列の生成（D の傷の本体）
2. 2次元累積和
3. **abc461 D を自力AC**
4. 計算量の GO/NO-GO 判断（C の反省）

> 計算量メモ：**足し算でなく一番重い項だけ残る**。`N=___ → 許容=O(___) ／ 方針=O(___) → GO/NO-GO` を全問で書く。詳細 [[complexity-estimation]]。

---

## Day A — 水 6/10（軽め・病み上がりなので無理しない）2次元配列を作る＋2次元累積和
- 体調次第。ダルければ「グリッド読込3通り」だけでも可
- セット1：abc461 D のグリッド読込を3通り（`bytes().map(|b|(b-b'0') as i64)` / 二重for / `chars()`+`to_digit`）、`vec![vec![0i64;w];h]`、`println!("{:?}",grid)`で確認
- セット2（余力があれば）：鉄則 **a08**（2次元累積和）
  - `pre[i+1][j+1] = pre[i][j+1] + pre[i+1][j] - pre[i][j] + a[i][j]`
  - 長方形和 `pre[r2][c2]-pre[r1][c2]-pre[r2][c1]+pre[r1][c1]`

## Day B — 木 6/11（フル）★今週の本丸：D を倒す
- ウォームアップ：水の2次元累積和をソラ書き
- もし水に a08 を飛ばしていたら、まず a08 → b08
- **セット1：abc461 D を自力AC**
  - 方針：行ペア (r1,r2) 固定 → 1次元 col[j] に潰す → 和=K を数える
  - 縦累積和 `colpre[i+1][j] = colpre[i][j] + grid[i][j]`
  - 「和=K の数え方」は drill **b15 の two-sum / seen so far** と同じ HashMap
  - 計算量実演：4重ループ=6×10¹⁰=TLE、行ペア+1次元=1.25×10⁸=GO
  - 通ったら提出して **AC 確認**（今週の最重要チェック）
- **セット2：1次元累積和の素振り** 鉄則 **a07 / b07**（D の「数える部分」を単体で固める）

## Day C — 金 6/12（軽）定着＋C対策ちょい
- ソラ書き：グリッド生成 / 2次元累積和 / 縦累積和 / 1次元和=K を参照なしで
- 余力：abc461 C を自力再実装（色リーダー枠M個＋自由枠K-M個に分離、`sort_by(|a,b| b.cmp(a))`、合計 i64）

## Day D — 土 6/13（軽・コンテストがあれば本番優先）
- コンテスト前ルーティン（開始前に `cargo compete new` 空打ちで認証確認）
- コンテスト本番、または過去問 C/D を1問タイマー計測
- 余力で `topics/` の [[2d-grid-and-prefix]] を起こす（🕳→✅）

---

## 今週の成功条件（縮小版）
- [ ] abc461 D が AC（木）← 最優先
- [ ] 2次元配列の生成・累積和を参照なしでソラ書きできる
- [ ] 全問で計算量 GO/NO-GO を書いた

## 来週に送るもの（記録だけ）
- 2次元 imos（a09/b09）
- 二分探索 / DP（ナップザック）/ BFS・DFS — [[competitive-rust-index]] の🕳

## 緑目標への影響
6週スプリントで3日（うちフル2日）の損失は痛いが致命傷ではない。
核（C安定・Dに手）の積み上げは続く。**焦って詰め込み再発させる方が損**。木のフル日でDを倒せば軌道に戻る。
