use proconio::input;

fn main() {
    input! {
        n: i64,
    }
    let bit[i64] = &n.iter.
    let mut ans = i64::new;
    for i in 0..bit.length{
        ans =+ bit[i];
    }
    println!("{}", ans);
}
